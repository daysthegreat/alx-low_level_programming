#include <stdio.h>

/**
 *main - assign random number to variable
 *print number stored in the variable n
 *Return: Always 0 (Success)
 */
int main(void)
{
int n;

for (n = 0; n < 10; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
